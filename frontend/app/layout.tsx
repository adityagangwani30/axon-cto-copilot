import type { Metadata, Viewport } from 'next'
import { Inter, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const inter = Inter({ 
  subsets: ["latin"],
  variable: "--font-inter"
});
const geistMono = Geist_Mono({ 
  subsets: ["latin"],
  variable: "--font-geist-mono"
});

export const metadata: Metadata = {
  title: 'Axon CTO Copilot | AI-Powered Engineering Intelligence for CTOs',
  description: 'Axon transforms reactive engineering management into autonomous, proactive intelligence. Get synthesized insights, automated incident response, and decision memory for your engineering team.',
  generator: 'v0.app',
  keywords: ['CTO', 'engineering', 'AI', 'automation', 'incident response', 'DevOps', 'productivity'],
  openGraph: {
    title: 'Axon CTO Copilot | AI-Powered Engineering Intelligence',
    description: 'Transform your engineering management with autonomous, proactive intelligence.',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className={`${inter.variable} ${geistMono.variable} font-sans antialiased`}>
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
